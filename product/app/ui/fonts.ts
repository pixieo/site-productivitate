import { Gotu, Cormorant_Garamond, Noto_Sans } from "next/font/google";

export const gotu = Gotu({
  weight: ["400"],
  subsets: ["latin"],
});

export const cormorantGaramond = Cormorant_Garamond({
  weight: ["400"],
  subsets: ["latin"],
});

export const noto = Noto_Sans({
    weight: ['300'],
  subsets:['latin'],
});
